name: $(MajorVersion).$(MinorVersion).$(date:yyMM).$(DayOfMonth)$(rev:rrr)

trigger:
  batch: true
  branches:
    include:
      - dev/*
      - main
  tags:
    exclude:
      - '*'
  
variables:
  isRelease: ${{ startsWith(variables['Build.SourceBranch'], 'refs/tags/r') }}
  MajorVersion: 0
  MinorVersion: 0

pool:
  name: Gerald-Local-Linux

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      Write-Host "Source Branch $(Build.SourceBranch)"
      Write-Host "Source Branch Name $(Build.SourceBranchName)"
      $branch = '$(Build.SourceBranch)'
      $branchName = '$(Build.SourceBranchName)'
      if ($branch.StartsWith("refs/tags/r")) {
        $major = [int]$branchName.Split(".")[0].substring(1)
        $minor = [int]$branchName.Split(".")[1]
        $revision = [int]$branchName.Split(".")[2]
        $buildNumber = "$major.$minor.$revision.0"
        Write-Host "##vso[task.setvariable variable=MajorVersion;]$major"
        Write-Host "##vso[task.setvariable variable=MinorVersion;]$minor"
        Write-Host "Setting build number to $buildNumber"
        Write-Host "##vso[build.updatebuildnumber]$buildNumber"
      }
  displayName: 'Set build number'
- template: setversion.yml
- task: NodeTool@0
  displayName:  Using latest NodeJS 20.x version
  inputs:
    versionSource: 'spec'
    versionSpec: '20.x'
    checkLatest: true
- task: Npm@1
  displayName: npm install
  inputs:
    workingDir: src/FrontendLibraries/ClientApp/
    verbose: false
- template: npm-publish-to-npmjs.yml
  parameters:
    npmCommand: 'build:shared-services'
    buildWorkingDirectory: 'src/FrontendLibraries/ClientApp/'
    publishWorkingDirectory: 'src/FrontendLibraries/ClientApp/dist/meshmakers/shared-services'
    libName: 'shared-services'
    isRelease: ${{ variables.isRelease }}
- template: npm-publish-to-npmjs.yml
  parameters:
    npmCommand: 'build:shared-auth'
    buildWorkingDirectory: 'src/FrontendLibraries/ClientApp/'
    publishWorkingDirectory: 'src/FrontendLibraries/ClientApp/dist/meshmakers/shared-auth'
    libName: 'shared-auth'
    isRelease: ${{ variables.isRelease }}
- template: npm-publish-to-npmjs.yml
  parameters:
    npmCommand: ' build:shared-ui'
    buildWorkingDirectory: 'src/FrontendLibraries/ClientApp/'
    publishWorkingDirectory: 'src/FrontendLibraries/ClientApp/dist/meshmakers/shared-ui'
    libName: 'shared-ui'
    isRelease: ${{ variables.isRelease }}
- template: npm-publish-to-npmjs.yml
  parameters:
    npmCommand: ' build:octo-services'
    buildWorkingDirectory: 'src/FrontendLibraries/ClientApp/'
    publishWorkingDirectory: 'src/FrontendLibraries/ClientApp/dist/meshmakers/octo-services'
    libName: 'octo-services'
    isRelease: ${{ variables.isRelease }}
- template: npm-publish-to-npmjs.yml
  parameters:
    npmCommand: ' build:octo-ui'
    buildWorkingDirectory: 'src/FrontendLibraries/ClientApp/'
    publishWorkingDirectory: 'src/FrontendLibraries/ClientApp/dist/meshmakers/octo-ui'
    libName: 'octo-ui'
    isRelease: ${{ variables.isRelease }}