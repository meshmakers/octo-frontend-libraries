mutation updateDashboard($entities: [SystemUIDashboardInputUpdate]!) {
  runtime {
    systemUIDashboards {
      update(entities: $entities) {
        rtId
        ckTypeId
        rtWellKnownName
        name
        description
        columns
        rowHeight
        gap
        rtChangedDateTime
      }
    }
  }
}
