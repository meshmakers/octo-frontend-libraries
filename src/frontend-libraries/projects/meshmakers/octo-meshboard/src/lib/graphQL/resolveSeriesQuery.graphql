query resolveSeriesQuery($input: ResolveSeriesQueryInput!) {
  streamData {
    resolveSeriesQuery(input: $input) {
      archiveRtId
      effectiveBucketMs
      points
      reducingFunction
      signal
      actualPoints
      diagnostic
    }
  }
}
